{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Capabilities for the main window. core:default is Tauri's own baseline (window handle, no filesystem/dialog/shell access); nothing app-specific is needed until Phase 2 adds a native sink.",
  "windows": ["main"],
  "permissions": ["core:default"]
}
